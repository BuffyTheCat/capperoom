$('.tooltipstered').tooltipster({
    animation: 'fade',
    delay: 200,
    theme: ['tooltip_blue'],
    side: 'top',
    trigger: 'hover'
});

$('.tooltip_rate').tooltipster({
    animation: 'fade',
    delay: 200,
    theme: ['tooltip_rateblue'],
    side: 'top',
    trigger: 'hover'
});
